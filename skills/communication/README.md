# Communication Skills (Local Registry)

All communication skills are first-party and shipped inside this repository under `skills/communication/**`.
No external registry is required to install or run them; OpenClaw loads them directly from the local registry.
