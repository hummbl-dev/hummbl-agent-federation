{
  "version": "0.2.0",
  "description": "Secret handling policy with API fee restrictions",

  "allowedSecrets": [
    "DISCORD_TOKEN",
    "TWILIO_AUTH_TOKEN",
    "TWILIO_ACCOUNT_SID",
    "OPENCLAW_API_KEY",
    "CLAWDBOT_TOKEN"
  ],

  "restrictedSecrets": {
    "description": "These secrets incur API fees - require explicit approval to use",
    "requiresApproval": true,
    "approvalEnv": "OPENCLAW_ALLOW_API_FEES",
    "secrets": [
      "OPENAI_API_KEY",
      "ANTHROPIC_API_KEY",
      "XAI_API_KEY",
      "OPENCLAW_OPENAI_API_KEY",
      "OPENCLAW_ANTHROPIC_API_KEY"
    ]
  },

  "requiredPrefix": "",

  "policy": {
    "noLogging": true,
    "noPersistence": true,
    "noPromptInjection": true,
    "blockApiKeysWithoutApproval": true
  },

  "notes": [
    "API keys (OpenAI, Anthropic, XAI) are RESTRICTED - they incur fees.",
    "Use subscription-based access (Claude.ai, ChatGPT Plus) instead.",
    "To use API keys: set OPENCLAW_ALLOW_API_FEES=1 (requires owner approval).",
    "See configs/cost-policy.json for full cost control policy."
  ]
}
