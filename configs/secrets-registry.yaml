# refs only; no values
openai:
  dev:
    secret_ref: "kv://openai/dev/api_key"
    scope: ["llm:chat"]
    max_ttl_seconds: 3600
  ci:
    secret_ref: "kv://openai/ci/api_key"
    scope: ["llm:chat"]
    max_ttl_seconds: 900
anthropic:
  dev:
    secret_ref: "kv://anthropic/dev/api_key"
    scope: ["llm:chat"]
    max_ttl_seconds: 3600
xai:
  dev:
    secret_ref: "kv://xai/dev/api_key"
    scope: ["llm:chat"]
    max_ttl_seconds: 3600
